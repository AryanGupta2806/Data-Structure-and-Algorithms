/*
class Node {
  public:
    int data;
    Node* next;

    Node(int x) {
        data = x;
        next = NULL;
    }
}; */

class Solution {
  public:
    bool searchKey(Node* head, int key) {
       Node* abc=head;
       while(abc!=NULL)
       {
           if(abc->data==key)
           return true;
           abc=abc->next;
       }
       return false;
        
    }
};
